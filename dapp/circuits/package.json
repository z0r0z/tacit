{
  "name": "tacit-circuits",
  "version": "0.1.0",
  "private": true,
  "description": "Tacit mixer-pool withdrawal circuit (forked from Tornado Cash, adapted per SPEC §3.8).",
  "type": "module",
  "scripts": {
    "build": "bash build.sh",
    "prove:sample": "node prove-sample.mjs",
    "verify:sample": "node verify-sample.mjs",
    "clean": "rm -rf build artifacts"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7",
    "ffjavascript": "^0.3.0",
    "snarkjs": "^0.7.4"
  }
}
