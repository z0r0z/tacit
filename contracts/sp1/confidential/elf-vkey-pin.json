{
  "_comment": "Binds the committed confidential SP1 guest ELFs to the verifying keys they must be deployed with. Two ELFs from one `cargo prove build`: elf/cxfer-guest (the settle guest → program_vkey, immutable PROGRAM_VKEY) and elf/reflection-prover (the Bitcoin-state relay prover → bitcoin_relay_vkey, immutable BITCOIN_RELAY_VKEY). verify-vkey-pin.sh asserts both sha256s on every PR. A rebuild that changes either ELF MUST regenerate its vkey + update this file IN THE SAME COMMIT, then redeploy ConfidentialPool with the new immutable vkey — otherwise every settle (resp. every attestBitcoinStateProven) reverts. Each vkey is the key a REAL Groth16 proof of THAT ELF verifies against on-chain (test/ConfidentialSwapProofReal.t.sol, test/ConfidentialReflectionProofReal.t.sol). The two share cxfer-core, so a cxfer-core change rebuilds BOTH — committing the ELFs is what keeps the prover box running the pinned bytes rather than a drifting rebuild.",
  "elf_sha256": "4ee1255691281fe4323f692916461e404929d00272642d376c97c02cd62d438c",
  "elf_bytes": 462896,
  "program_vkey": "0x00bc5661436d99a5beaed7d7c5d9c99ceb9f67c1c42d74c845d97fd83874c93d",
  "reflection_elf_sha256": "a11fa8a536062ab4cd6f686f8bc5ce3ab40a2ca513af417c4e2fe001f4198cc3",
  "reflection_elf_bytes": 339704,
  "bitcoin_relay_vkey": "0x00be458f63109d0ad222e97053571dca5f64154ce9638a8278d57c2842256fda",
  "guest_state": "Settle guest (cxfer-guest): complete gen-1 op set (OP_WRAP/TRANSFER/UNWRAP/BRIDGE_BURN/BRIDGE_MINT/ATTEST_META) + OP_SWAP (confidential AMM batch; amounts bound by direct secp Pedersen openings, ~513K cycles/intent). Reflection prover (reflection-prover): folds confirmed Bitcoin confidential-pool effects (public on-chain CXFER envelopes + headers) into BitcoinReflectionPublicValues; its proof is the sole authority for attestBitcoinStateProven. Both built via one `cargo prove build`; each vkey == the key its real Groth16 verifies against on-chain. The previous reflection vkey 0x00116c02 drifted to 0x00be458f when the OP_SWAP cxfer-core change rebuilt the shared crate — the pinned reflection ELF here is the canonical one."
}
